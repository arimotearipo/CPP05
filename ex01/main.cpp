#include "Bureaucrat.hpp"

using std::cout;
using std::endl;

int	main(void)
{
	Bureaucrat a("Ali", 100);
	// Form b("B", 75, 75);
	// Form c("C", 125, 125);

	// a.signForm(b);
	// a.signForm(c);
	return (0);
}